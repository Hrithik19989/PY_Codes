Extensions to try next

Hint system — after 3 wrong guesses, reveal if the number is odd/even
Leaderboard — store top 5 scores per difficulty instead of just the best
Timer — use time.time() to track how fast the player guesses
Two-player mode — player 1 types the secret number (hidden), player 2 guesses
Difficulty system - Easy , Medium Hard using tuples
Score calculator based on predictions and no . of attempts
High scorer stored using json and os module

